# template-gallery-prototype

To test locally
```
cd pages-build
npm start
```

To build
```
cd pages-build
npm run build
```

To publish to gh-pages  ( beware this does a git commit and git push )
```
cd pages-build
npm run deploy
```
